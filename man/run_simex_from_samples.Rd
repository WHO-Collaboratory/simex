% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_simex.R
\name{run_simex_from_samples}
\alias{run_simex_from_samples}
\title{Simulate forward from posterior samples}
\usage{
run_simex_from_samples(
  samples,
  time = 0:200,
  modification = NULL,
  start_from_snapshot = NULL
)
}
\arguments{
\item{samples}{Object returned by \code{\link[monty:monty_sample]{monty::monty_sample()}} (must carry
\code{attr(samples, "packer")}).}

\item{time}{Time indices to simulate, passed to \code{\link[=run_simex]{run_simex()}}.}

\item{modification}{Optional list of parameter overrides applied after
unpacking (structure depends on \code{packer$groups}).}

\item{start_from_snapshot}{If not \code{NULL}, index of a saved snapshot
used as the initial state instead of the default.}
}
\description{
Unpacks parameters from a \code{monty} samples object (with \code{packer}
attribute) and calls \code{\link[=run_simex]{run_simex()}}.
}
