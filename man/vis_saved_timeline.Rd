% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_saved_timeline.R
\name{vis_saved_timeline}
\alias{vis_saved_timeline}
\title{Timeline for one outcome across saved scenarios}
\usage{
vis_saved_timeline(
  simexl,
  outcome = c("Cases", "Hospitalisations", "Deaths"),
  what = c("incidence", "prevalence"),
  stratify_by_age = FALSE,
  use_absolute_numbers = TRUE,
  show_ribbon = FALSE,
  cri_alpha = 0.75,
  type = c("highchart"),
  period_days = 7L
)
}
\arguments{
\item{simexl}{Named list of simex objects (e.g. from the Shiny app).}

\item{outcome}{One of \code{"Cases"}, \code{"Hospitalisations"}, \code{"Deaths"}.}

\item{what}{Passed to \code{\link[=extract]{extract()}}: \code{"incidence"} or \code{"prevalence"}.}

\item{stratify_by_age}{If \code{FALSE}, ages are summed; if \code{TRUE}, one small
multiple per age (like the fit tab), each showing all scenarios.}

\item{use_absolute_numbers}{If \code{FALSE}, values are divided by total
population \code{N} from the first simex (same idea as \code{\link[=plot.simex]{plot.simex()}}).}

\item{show_ribbon}{If \code{TRUE} and there are multiple particles, show
credible intervals from \code{\link[=extract]{extract()}} with \code{cri = TRUE}.}

\item{cri_alpha}{Width of central interval when \code{show_ribbon} is true
(default \code{0.75}).}

\item{type}{Currently only \code{"highchart"}.}

\item{period_days}{Passed to \code{\link[=extract]{extract()}} when \code{what = "incidence"} (default
\code{7L} for weekly totals). Ignored for prevalence.}
}
\value{
A \code{highchart} object, or a list of them when
\code{stratify_by_age} is \code{TRUE}. \code{NULL} if \code{simexl} is empty or \code{NULL}.
}
\description{
Builds a highcharter line chart (or one chart per age group) comparing
named simex objects, using \code{\link[=extract]{extract()}} for incidence or prevalence in a
single compartment (cases = E, hospitalisations = H, deaths = D).
}
\author{
Finlay Campbell
}
