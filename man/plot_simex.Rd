% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_simex.R
\name{plot_simex}
\alias{plot_simex}
\title{Unified plots for one or more simex scenarios}
\usage{
plot_simex(
  x,
  mode = c("timeline", "endpoint"),
  renderer = c("ggplot", "highcharter"),
  what = c("prevalence", "incidence"),
  compartments = c("S", "E", "C", "H", "R", "D"),
  stratify_by_age = FALSE,
  show_ribbon = TRUE,
  cri_alpha = 0.75,
  period_days = 1L,
  data = NULL,
  log = FALSE,
  freescales = TRUE,
  base_size = 11,
  cri_display_alpha = 1
)
}
\arguments{
\item{x}{A single \code{simex} object or a \strong{named} list of simex objects
(scenarios). A single object is wrapped as \code{Model = x}. Unnamed list
elements get names \code{scenario_1}, ...}

\item{mode}{\code{"timeline"} (values vs time) or \code{"endpoint"} (incidence summed
over the full time range, stratified by age).}

\item{renderer}{\code{"highcharter"} or \code{"ggplot"}.}

\item{what}{For \code{mode = "timeline"}: \code{"incidence"} or \code{"prevalence"}.
Ignored for \code{mode = "endpoint"} (always incidence).}

\item{compartments}{Character vector of compartment \strong{letters} (e.g.
\code{c("E", "H", "D")}). For \code{mode = "endpoint"}, must have length 1. For
\code{renderer = "highcharter"} and \code{mode = "timeline"}, if more than one
compartment is given, only the first is used (with a warning).}

\item{stratify_by_age}{If \code{TRUE} (timeline only), ages are kept separate
using facets in \strong{ggplot} (\code{facet_grid} when multiple compartments,
\code{facet_wrap} otherwise). For \strong{highcharter}, one chart per age stratum
is still returned as a list. If \code{FALSE}, ages are summed inside
\code{\link[=extract.simex]{extract.simex()}}.}

\item{show_ribbon}{If \code{TRUE}, request CRI columns from \code{extract} and draw
ribbons (timeline) or error bars (endpoint) when multiple particles
exist.}

\item{cri_alpha}{Central interval width for \code{extract(..., cri = TRUE)}.}

\item{period_days}{Passed to \code{\link[=extract.simex]{extract.simex()}} (incidence period aggregation).}

\item{data}{Optional observed \strong{incidence} (\code{time}, \code{age}, \code{compartment},
\code{value}). \strong{Only used when \code{mode = "timeline"}}; if \code{mode = "endpoint"}
and \code{data} is not \code{NULL}, a warning is issued and \code{data} is ignored.}

\item{log, freescales, base_size}{Passed through to ggplot timeline/endpoint
where relevant (\code{freescales} applies to timeline facets).}

\item{cri_display_alpha}{Line alpha for ggplot endpoint error bars only.}
}
\value{
A ggplot or highchart object, or a \strong{list} of highcharts when
\code{mode = "timeline"}, \code{renderer = "highcharter"}, and
\code{stratify_by_age = TRUE} (ggplot age stratification uses facets instead).
\code{NULL} when there is nothing to plot.
}
\description{
Builds all model series via \code{\link[=extract.simex]{extract.simex()}} (including \code{period_days},
\code{cri}, and \code{stratify_by}). Renders with \strong{ggplot2} or \strong{highcharter}
depending on \code{renderer}.
}
\examples{
\dontrun{
pars <- get_parameters()
sx <- run_simex(pars, time = 0:50, n_particles = 4)
plot_simex(sx,
  mode = "timeline", renderer = "ggplot",
  what = "incidence", compartments = c("E", "D")
)
plot_simex(list(baseline = sx),
  mode = "endpoint", renderer = "ggplot",
  compartments = "E"
)
}

}
