% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_saved_fit.R
\name{vis_saved_fit}
\alias{vis_saved_fit}
\title{Fit plot across saved scenarios vs observed data}
\usage{
vis_saved_fit(
  simexl,
  data,
  stratify_fit_by_age = TRUE,
  use_absolute_numbers = TRUE,
  show_ribbon = FALSE,
  cri_alpha = 0.75,
  period_days = 7L
)
}
\arguments{
\item{simexl}{Named list of simex objects.}

\item{data}{Data frame with columns \code{time}, \code{age}, \code{value}.}

\item{stratify_fit_by_age}{If \code{TRUE}, return a list of highcharts (one per
age); if \code{FALSE}, one chart with all ages summed.}

\item{use_absolute_numbers}{Passed through to axis labelling (values are
not re-scaled here; ensure data match model scale).}

\item{show_ribbon}{If \code{TRUE}, show credible intervals per scenario when
particles allow.}

\item{cri_alpha}{Width of central interval (default \code{0.75}).}

\item{period_days}{Passed to \code{\link[=extract]{extract()}} for model series and used to sum
observed counts over the same periods (default \code{7L}).}
}
\value{
One \code{highchart} or a list of them (when \code{stratify_fit_by_age} is
\code{TRUE}). \code{NULL} if \code{simexl} is empty or \code{data} is \code{NULL}.
}
\description{
Compares community plus hospital incidence from each simex object to the
same observed series, using \code{\link[=extract]{extract()}} with the same logic as
\code{\link[=plot.simex]{plot.simex()}} \code{format = "fit"}. Model curves are coloured by scenario.
}
\author{
Finlay Campbell
}
