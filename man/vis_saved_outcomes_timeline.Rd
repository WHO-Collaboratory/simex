% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_saved_outcomes_timeline.R
\name{vis_saved_outcomes_timeline}
\alias{vis_saved_outcomes_timeline}
\title{Timeline for one outcome (E, H, or D) with optional incidence data}
\usage{
vis_saved_outcomes_timeline(
  simexl,
  outcome = c("Cases", "Hospitalisations", "Deaths"),
  what = c("incidence", "prevalence"),
  stratify_by_age = FALSE,
  data = NULL,
  use_absolute_numbers = TRUE,
  show_ribbon = FALSE,
  cri_alpha = 0.75,
  period_days = 7L
)
}
\arguments{
\item{simexl}{Named list of simex objects, or \code{list()} for data-only.}

\item{outcome}{One of \code{"Cases"}, \code{"Hospitalisations"}, \code{"Deaths"}.}

\item{what}{\code{"incidence"} or \code{"prevalence"} for the model trajectories.}

\item{stratify_by_age}{If \code{FALSE}, ages are summed. If \code{TRUE}, one chart per
age (ages from the model if present, otherwise from \code{data}).}

\item{data}{Optional data frame with columns \code{time}, \code{age}, \code{compartment},
and \code{value} (incidence).}

\item{use_absolute_numbers}{If \code{FALSE}, model values are divided by \code{N}
from the first simex (ignored when there is no model).}

\item{show_ribbon}{Credible intervals when multiple particles exist.}

\item{cri_alpha}{Width of central interval (default \code{0.75}).}

\item{period_days}{Passed to \code{\link[=extract]{extract()}} for model series when
\code{what = "incidence"}; observed incidence is summed over the same periods
(default \code{7L} for weekly totals). Use \code{1L} for daily values.}
}
\value{
A single \code{highchart} when \code{stratify_by_age} is \code{FALSE}, or a list
of highcharts when \code{TRUE}. \code{NULL} if there is nothing to plot.
}
\description{
Shows model trajectories for a single compartment when \code{simexl} is non-empty,
and/or observed incidence points when \code{what = "incidence"} and \code{data} is
supplied. Data-only plots are supported (empty \code{simexl} or \code{list()}).
}
\author{
Finlay Campbell
}
