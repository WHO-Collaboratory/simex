% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_simex.R
\name{fit_simex}
\alias{fit_simex}
\title{Bayesian fit of the simex model to incidence data}
\usage{
fit_simex(
  data,
  parameters = get_parameters(),
  priors = get_priors(p_trans = list(dist = "Beta", a = 1, b = 1)),
  settings = get_settings(),
  samples = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} with columns \code{time}, \code{age}, \code{compartment}, and
\code{value} (incidence counts). Rows with \code{time <= 0} are dropped (dust expects
strictly increasing times from first observation). \code{age} is coerced to an
ordered factor via \code{\link[forcats:fct_inorder]{forcats::fct_inorder()}}.}

\item{parameters}{Named list of fixed model parameters; defaults to
\code{\link[=get_parameters]{get_parameters()}}. Entries named in \code{priors$parameters} are treated as
fitted scalars instead.}

\item{priors}{A Monty model from \code{\link[=get_priors]{get_priors()}} listing fitted parameters.}

\item{settings}{A list from \code{\link[=get_settings]{get_settings()}} controlling chains, particles,
and sampler behaviour.}

\item{samples}{Optional previous \code{monty} samples used to initialise proposal
covariance when supported by the sampler branch.}
}
\value{
A \code{monty} samples object with attribute \code{packer} (Monty packer
mapping fitted names to the dust likelihood).
}
\description{
Builds a dust2 filter from long-format observations, combines it with a
Monty prior model from \code{\link[=get_priors]{get_priors()}}, and runs \code{\link[monty:monty_sample]{monty::monty_sample()}}.
}
